import React from "react";

import { ListWrapper, ListContainer } from "./List.styled";

const List = () => {
  return (
    <ListWrapper>
      <ListContainer>
        THIS IS LIST
      </ListContainer>
    </ListWrapper>
  );
};

export default List;
