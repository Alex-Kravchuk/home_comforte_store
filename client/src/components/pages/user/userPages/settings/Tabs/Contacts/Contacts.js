import React from "react";
import { ContactsContainer, ContactsWrapper } from "./Contacts.styled";

const Contacts = () => {
  return (
    <ContactsWrapper>
      <ContactsContainer>Contacts settings</ContactsContainer>
    </ContactsWrapper>
  );
};

export default Contacts;
