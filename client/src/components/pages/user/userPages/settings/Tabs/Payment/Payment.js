import React from 'react';
import { PaymentContainer, PaymentWrapper } from './Payment.styled';

const Payment = () => {
	return (
	  <PaymentWrapper>
		<PaymentContainer>Payment Settings page</PaymentContainer>
	  </PaymentWrapper>
	)
 }

export default Payment;